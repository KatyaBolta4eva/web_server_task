<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <title><%= title %></title>
</head>
<body>
<% if (created) {%>
    <div class="alert alert-success">
        Note has been created!
    </div>

<% } %>
<div class="container">
    <h1><%= title %></h1>

    <form action="/" method="post" class="mb-2">
        <div class="form-group mb-2">
            <input type="text" class="form-control" id="name" name="title" required>
        </div>

        <button type="submit" class="btn btn-primary">Create!</button>
    </form>
    <% if (notes.length) {%>

    <div class="card">
        <ul class="list-group">
            <% for (let i = 0; i < notes.length; i++) { %>
                <li class="list-group-item d-flex justify-content-between align-items-center" id="note-<%= notes[i].id %>">
                    <span class="note-title"><%= notes[i].title %></span>
                    <div class="btn-group">
                        <button class="btn btn-warning btn-sm me-1" data-type="edit" data-id="<%= notes[i].id %>">
                            Edit
                        </button>
                        <button class="btn btn-danger btn-sm" data-type="remove" data-id="<%= notes[i].id %>">
                            &times;
                        </button>
                    </div>
                </li>
            <% } %>
        </ul>
    </div>
<% } else {%>
    <p>No notes!</p>
    <% } %>
</div>


<script src="/app.js"></script>
</body>
</html>